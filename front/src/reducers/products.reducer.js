const initialState = [
    {
        id: 1,
        name: 'Fancy shoes',
        price: 2450,
    },
];

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}
